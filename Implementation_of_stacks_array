int top = -1 , st[10];

int push(x)
{
  if(top >= n) return 0;
  top = top +1;
  st[top] = x;
}

int top()
{
  if(top == -1) return 0;
  return st[top];
}

int pop()
{
  if(top == -1) return 0;
  top = top-1;
}

size()
{
  return top+1;
}
  
