push(x)
{
  s1.push(x);
}

top()
{
  if(s2 != empty)
    s2.top();
  else
    s2.push(s1.top());
    s2.top();
}

pop()
{
  if(s2 != empty)
    s2.pop();
  else
    s2.push(s1.top());
    s2.pop();
}
