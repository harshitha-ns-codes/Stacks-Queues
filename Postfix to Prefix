#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    string PostfixToPrefix(string s)
    {

        stack<string> st;
        for(int i = 0; i < s.length(); i++)
        {
            // Operand
            if((s[i] >= 'A' && s[i] <= 'Z') ||
               (s[i] >= 'a' && s[i] <= 'z') ||
               (s[i] >= '0' && s[i] <= '9'))
            {
                st.push(s[i]);
            }

            // Opening bracket
            else 
            {
                t1 = st.top(); st.pop();
                t2 = st.top(); st.pop();
                st.push(s[i]+t2+t1);
    }
return st.top();
};

int main()
{
    Solution obj;

    string s = "AB-DE+F*/";

    cout << obj.infixToPrefix(s);

    return 0;
}
